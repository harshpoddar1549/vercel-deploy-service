{
  "name": "vercel-deploy-service",
  "version": "1.0.0",
  "description": "Deploy service which will read the redis queue for any id and if it's there, it will perform the build and store the built files inside my r2 storage /built/{id} folder",
  "main": "app.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./dist/app.js",
    "dev": "tsc-watch --onSuccess 'node --trace-warnings ./dist/app.js'"
  },
  "author": "harshpoddar1549@gmail.com",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.525.0",
    "@aws-sdk/credential-providers": "^3.525.0",
    "@aws-sdk/lib-storage": "^3.588.0",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "redis": "^4.6.13"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "tsc-watch": "^6.0.4",
    "typescript": "^5.3.3"
  }
}
